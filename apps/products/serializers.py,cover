> from rest_framework import serializers
  
> from apps.organizations.serializers import BranchSmallSerializer, CostCenterSmallSerializer
> from .models import ProductCategory, UnitOfMeasure, Product, BranchProduct
  
  
> class ProductCategorySmallSerializer(serializers.ModelSerializer):
>     class Meta:
>         model = ProductCategory
>         fields = ["uuid", "name"]
  
  
> class UnitOfMeasureSmallSerializer(serializers.ModelSerializer):
>     class Meta:
>         model = UnitOfMeasure
>         fields = ["uuid", "code", "name"]
  
  
> class ProductSmallSerializer(serializers.ModelSerializer):
>     category_detail = ProductCategorySmallSerializer(source="category", read_only=True)
>     unit_detail = UnitOfMeasureSmallSerializer(source="unit", read_only=True)
  
>     class Meta:
>         model = Product
>         fields = [
>             "uuid",
>             "name",
>             "sku",
>             "barcode",
>             "internal_code",
>             "category_detail",
>             "unit_detail",
>             "requires_lot",
>             "requires_expiration_date",
>             "is_medication",
>             "is_controlled",
>             "is_active",
>         ]
  
  
> class ProductCategorySerializer(serializers.ModelSerializer):
>     class Meta:
>         model = ProductCategory
>         exclude = ["id", "deleted_at"]
  
  
> class UnitOfMeasureSerializer(serializers.ModelSerializer):
>     class Meta:
>         model = UnitOfMeasure
>         exclude = ["id", "deleted_at"]
  
  
> class ProductSerializer(serializers.ModelSerializer):
>     category_detail = ProductCategorySmallSerializer(source="category", read_only=True)
>     unit_detail = UnitOfMeasureSmallSerializer(source="unit", read_only=True)
  
>     class Meta:
>         model = Product
>         exclude = ["id", "deleted_at"]
  
  
> class BranchProductSerializer(serializers.ModelSerializer):
>     branch_detail = BranchSmallSerializer(source="branch", read_only=True)
>     product_detail = ProductSmallSerializer(source="product", read_only=True)
>     cost_center_detail = CostCenterSmallSerializer(source="cost_center", read_only=True)
  
>     class Meta:
>         model = BranchProduct
>         exclude = ["id", "deleted_at"]
